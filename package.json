{
  "name": "appnode",
  "version": "1.0.0",
  "description": "Simple Node.js app on port 3000",
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p build && cp public/index.html build/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
